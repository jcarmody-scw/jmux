/**
 * Input Diagnostics Page — stub.
 *
 * Full implementation pending wterm migration (uses WTerm.element.querySelector('textarea')
 * for event instrumentation). Route preserved so the URL doesn't 404.
 */
export default function InputDiagnostics() {
  return (
    <div style={{ padding: '2rem', color: 'var(--text)' }}>
      <h1 style={{ marginBottom: '1rem' }}>Input Diagnostics</h1>
      <p style={{ color: 'var(--text-secondary)' }}>
        Temporarily unavailable during wterm migration.
      </p>
    </div>
  )
}
